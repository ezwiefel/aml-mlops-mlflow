name: Linting and Testing

on:
  pull_request:
    branches: [ main, develop ]

  workflow_dispatch:

jobs:
  Lint-and-Test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Lint
        run: flake8
  